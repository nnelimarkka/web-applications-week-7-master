extends layout

block content
  h1= title
  h3(id="secrets")
  p Welcome to #{title}
  
  form(action="" id="todo-form" method="POST")
    label(for="text-input") Add todos
    input(type="text" id="todo-input" name="text-input")
  
  h4 Todos:
  ul(id="todo-content")
  
  script(src="/javascripts/index.js") 
